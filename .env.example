# Copy to .env.local to choose which backend the app talks to.
# Without it, `npm run dev` uses http://localhost:8080/api and production builds
# use the deployed backend on Render.
VITE_API_BASE_URL=http://localhost:8080/api
